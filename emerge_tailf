#!/bin/sh

set -eu

while true
do
	running="$(qlop -c | awk '/^ \* / {print $2}')"

	[ -z "${running}" ] && break

	echo "${running}" | while read pkg
	do
		echo ">> ${pkg}"
		tailf -n0 "/var/tmp/portage/${pkg}/temp/build.log" || :
	done
done
